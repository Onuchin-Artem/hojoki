# Web edition — single self-contained page + assets.
# Also copies the downloadable editions (build/hojoki.pdf, build/hojoki.epub)
# into web/, where index.html links them.

.PHONY: all
all:
	python3 build_web.py --stage 2
